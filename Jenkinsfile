pipeline{
    agent{
        label "any"
    }
    stages{
        stage("Build"){
            steps{
                echo "welcome to tools demo"
                sh " mvn --version"
            }
        }
    }
 }
